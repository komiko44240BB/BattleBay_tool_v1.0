#include "readtraining.h"

#include <stddef.h>


void readAndApplyTraining(struct Object* o,char* training_file_name, unsigned int training_lvl) {
    if(o == NULL || training_file_name == NULL || training_lvl == 0){
        return;
    }
    
}
